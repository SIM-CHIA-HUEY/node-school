let fruit=('orange')
//if the length of fruit is longer than 5, to activate this function, add .length
if(fruit.length>5){console.log('The fruit name has more than five characters.')}
else{console.log('The fruit name has five characters or less.')}

/**Otherwise we use the if like this :  if (n > 1) {
       console.log('the variable n is greater than 1.')
     } else {
       console.log('the variable n is less than or equal to 1.')
     }
*/